#include "ObstacleSpawner.h"



//void ObstacleSpawner::Spawn() {
//    int screenWidth = Globals::screenWidth;
//    int screenHeight = Globals::screenHeight
//
//    // Generate random position off-screen
//    int x = rand() % screenWidth;
//    int y = rand() % screenHeight;
//    if (rand() % 2 == 0) {
//        x -= screenWidth;
//    }
//    else {
//        x += screenWidth;
//    }
//    if (rand() % 2 == 0) {
//        y -= screenHeight;
//    }
//    else {
//        y += screenHeight;
//    }
//
//    // Randomly choose direction
//    float dx = (rand() % 201 - 100) / 100.0f; // Random float between -1 and 1
//    float dy = (rand() % 201 - 100) / 100.0f; // Random float between -1 and 1
//
//    // Spawn enemy at generated position with chosen direction
//    SkillObstacle enemy = new SkillObstacle(x, y, dx, dy);
//    /* add enemy to game world */
//}

